name: Semgrep Scan

on: push

jobs:
  Scan-Build:
    runs-on: ubuntu-latest
    steps:
      - name: install semgrep and scan
        run: |
            echo "exploring"
            pwd
            sudo ls - al /home/
            sudo ls - al /home/runner/
            sudo ls - al /home/runner/work/
            sudo ls - al /home/runner/work/DVWA/
            sudo ls - al /home/runner/work/DVWA/DVWA
            python3 -m pip install semgrep
            echo "path: " 
            pwd
            echo "ls:" 
            ls -al
            semgrep --config=auto .


